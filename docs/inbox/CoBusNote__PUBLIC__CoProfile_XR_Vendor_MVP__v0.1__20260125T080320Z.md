# CoProfile XR Vendor MVP v0.1 (PUBLIC)

## Seed wedge
Ship a read-only overlay component (web-first) that shows:
- PetOwnerType card image (or SVG composition)
- minimal prefs summary (3–5 bullets)
- hide-now / revoke-now action

## Integration targets
- v0: Web overlay (WebView in Unity/Unreal; or browser source)
- v1: Unity package (C#) wrapper
- v2: Unreal plugin wrapper

## Allowed claim categories (seed)
- PREFS: yes (default)
- REP: no (unless explicit + attested)
- PROOFS: no (unless explicit expand + cryptographic verify)

END
