# Agent Terms (PUBLIC LATEST, seed-stage stub)
UTC=20260117T205811Z

Intent:
- Define what an AI agent may do by default (read/public).
- Define what requires PS7 receipt (write, submit PRs, request enhanced endpoints).

Rules:
A) Read-only access to public assets is allowed.
B) Any write or submission requires explicit PS7 receipt.
C) All changes must be reversible and produce receipts.

END
