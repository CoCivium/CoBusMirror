# CoStar Mobile Principles v0

- Mobile = thin-client of CoStacks
- Default mode = public_ephemeral
- Private mode = explicit, isolated, non-default
- Save / Export / Externalize are distinct operations
- No persistent hidden state
- All meaningful state must be pointerable or reproducible
- UI is derived, not authoritative
- Mobile never becomes the system of record
