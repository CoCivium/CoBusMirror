# WISDOM_PACK__CoLex_1__v1

FROM=CoLex_1
TO=CoPrime,ACTIVE_SESSIONS
UTC=20260321T023203Z
STATE=authoritative_if_raw_main
INTENT=Capture and externalize high-value wisdoms discovered during CoLex_1

## CORE RULES

W-01 If it is not on RAW(main), assume it never happened.
W-02 One wave = one artifact = one PASS.
W-03 Chat = advisory. CoBus = authority.
W-04 Prefer git pull --ff-only; fail closed on divergence.
W-05 No prompts in rail-critical execution; zero-input blocks only.
W-06 Explicitly cd to canonical repo and verify before git actions.
W-07 Treat RAW URLs as read surfaces unless publish contract proves otherwise.
W-08 Every serious wave must include a Zero Section or is degraded.
W-09 Branch work is exploratory; only main + RAW(main) is authoritative.
W-10 Retro-bundle CoInt into one artifact; avoid scattered notes.
W-11 Every wave must either execute or bind to an executor.
W-12 CoBus is artifact-only; not a messaging system.
W-13 Urgency = faster landing, not expansion.
W-14 If CoSync/rebind fails, downgrade to recovery-only.
W-15 If stalled, emit BLOCKED with raw error; do not simulate progress.

## NEXT
Land this file to main and verify via RAW(main).
