# BOOTSTRAP_ROLLUP__LATEST

Purpose: deterministic “where to look next” bootstrap rollup. Mandatory for fast fail-closed bootstraps.

Minimum contents (kept intentionally short):
- Pinned CoBeacon RAW URL (fetched first)
- Pinned Broadcast RAW URL
- Pinned PointerPack RAW URL
- Pinned MasterPlan RAW URL (once available)
- CanonPaths UNC/RAW (once available)
- CoShare root UNC + Quarantine base UNC (once available)
