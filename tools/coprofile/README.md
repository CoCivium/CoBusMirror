# tools/coprofile

Seed tools (no external deps):
- CoProfile-CardKey.ps1: generate deterministic card_key (BASE32-ish) from coprofile_id + prefs + theme_id
- CoProfile-GenSample.ps1: emit a sample bundle (seed)
- CoProfile-ValidateBundle.ps1: minimal invariant checks (not full JSON Schema validation)

END
